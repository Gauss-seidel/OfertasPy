# AGENTS.md — OfertasPy

Empty repository. No code, no commits, no branches.

## State
- Repo root (git): `C:\Users\ASUS` (no commits yet)
- Project root: `C:\Users\ASUS\Desktop\Proyectos\OfertasPy`
- No tracked files, no `.gitignore`, no CI, no build config.

## What to do first
1. Choose a Python project structure and toolchain.
2. Create a `.gitignore` for Python.
3. Initialize a proper git repo inside the project directory (`git init` here).
4. Define dev commands as they are introduced (test, lint, typecheck, etc.).

Update this file as the project grows.
